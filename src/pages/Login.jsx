// TODO: build the real sign-in form once an auth provider is chosen.
export default function Login() {
    return (
        <div>
            <h1>Sign in</h1>
            <p>Auth provider not configured yet.</p>
        </div>
    );
}
