// Admin-only: manage which users can access the app and their role
// (admin / operator / viewer). Depends on whichever auth provider is chosen.
export default function UserManage() {
    return (
        <div>
            <h1>User Management</h1>
            <p>Not implemented yet — depends on the chosen auth provider.</p>
        </div>
    );
}
