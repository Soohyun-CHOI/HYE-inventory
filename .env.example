# ---- Server-only variables (read by /api functions, NEVER exposed to the browser) ----
# Do NOT prefix these with VITE_ or they will be bundled into client-side code.

AIRTABLE_API_TOKEN=
AIRTABLE_BASE_ID=appUiNQ3dZ7aXBpg6

# Auth provider (choose one, fill in once decided):
# AUTH_PROVIDER_URL=
# AUTH_PROVIDER_SECRET=

# ---- Client-safe variables (must be prefixed with VITE_ to reach the browser) ----
# None needed yet. The frontend should only ever talk to our own /api routes,
# never to Airtable or the auth provider directly.
